# Sample documents contain real participant data (PII) — keep out of the repo
assets/*.pdf

# OS / editor cruft
Thumbs.db
ehthumbs.db
Desktop.ini
.DS_Store
*.swp

# Node / backend
node_modules/
# Backend local secrets — MUST NOT be committed (Graph client secret)
backend/local.settings.json
supabase/.env
.env

# Scratch / temp artifacts
_*.png
_*.pdf
_*.html
_*.mjs
_edge*/
_e*/
coords*.txt
